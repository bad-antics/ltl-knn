// ltl-knn/main.ltl
//
// Brute-force k-NN classifier with cross-validation.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-knn v0.1.0")
    io::println("Brute-force k-NN classifier with cross-validation.")
    0
}
