// examples/demo.ltl — runnable demo for ltl_knn

import std::io
use ltl_knn as lib

fn main() {
    io::println("ltl_knn demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}